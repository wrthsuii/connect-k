#  Гра "k в ряд" (Connect K)

> k в ряд "падають "розмір колодязю: n x m". Параметри: n>=5, m>=6, k=[3..5] - кількість в ряд для виграшу. +MinMax (Alpha–Beta). Визначаються заборонені комірки.

## Структура проєкту

```
connect-k/
├── haskell/
│   ├── game.hs          # Реалізація ігрової логіки та ШІ (MinMax) на Haskell
│   └── README.md        # Опис Haskell реалізації
├── python/
│   ├── game.py          # ООП-реалізація ігрової логіки та ШІ на Python
│   └── README.md        # Опис Python реалізації
├── web/
│   ├── index.html       # Головна сторінка фронтенду (UI)
│   ├── script.js        # Логіка фронтенду (взаємодія з бекендом, відмальовка)
│   └── style.css        # Стилі інтерфейсу
├── game.pl              # Основна логіка гри та ШІ на Prolog
├── server.py            # Flask-сервер (API), що з'єднує фронтенд із Prolog-логікою
├── COMPARISON.md        # Детальне порівняння трьох парадигм (Prolog, Python, Haskell)
└── README.md            # Загальний опис проекту та інструкції із запуску
```
## Особливості гри

* **Гнучкі параметри поля:** Налаштування кількості рядків ($n \ge 5$) та стовпців ($m \ge 6$).
* **Можливість змінити кількість фішок в ряд для виграшу:** Від $k=3$ до $k=5$ (горизонталь, вертикаль, діагоналі).
* **Гравітація:** Фішки "падають" у найнижчу доступну вільну клітинку стовпця.
* **Заборонені клітинки ("перешкоди"):** Можуть бути згенеровані випадково або розставлені вручну перед початком гри. Вони ігноруються при падінні фішок та блокують переможні лінії.
* **Режими гри:**
  * людина - людина
  * людина - 🤖 AI (на Prolog)
  * 🤖 AI - 🤖 AI
* **Штучний інтелект:** Використовує алгоритм **MinMax** з оптимізацією **Alpha-Beta відсікання**. Глибина пошуку (складність) налаштовується користувачем. Для оцінки позицій використовується евристика аналізу "вікон".

## Інструкція із запуску (Prolog + Flask)

### Вимоги:
1. Встановлений **Python 3.x**.
2. Встановлений **SWI-Prolog** (та доданий до змінної середовища PATH).

### Встановлення залежностей:
Відкрийте термінал у папці проєкту та виконайте команду для встановлення необхідних Python-бібліотек:
```bash
pip install flask pyswip
```

### Запуск сервера:
```bash
python server.py
```
Відкрийте браузер та перейдіть за адресою: **http://127.0.0.1:8001/**
  
## Реалізації

**[Prolog](game.pl)** - Логічне програмування
* Ігрові правила та стани як логічні предикати, декларативний опис алгоритму MinMax (Alpha-Beta).
* Мультипризначенність предикатів (перевірка/генерація ходів), автоматичний backtracking.


**[Python](python/)** - Об'єктно-орієнтовне / Імперативне програмування
* Ігрове поле як двовимірна матриця, інкапсуляція стану та логіки у класі `ConnectK`.
* Покрокове обчислення "вікон" з явним контролем стану та мутацією даних.


**[Haskell](haskell/)** - Функційне програмування
* Ігрове поле як список списків (`[[Int]]`), суворо незмінні (immutable) структури даних.
* Чисті функції, глибока рекурсія, використання функцій вищого порядку (`map`, `concatMap`).

## Презентація

## Автор: 
Титаренко Владислава, КН-3

## Ліцензія
*Реалізації створено для курсу "Логічне програмування" авторства Юрія Ющенка.*

*Дозволяється вільне використання та поширення матеріалу з вказанням авторства.*
